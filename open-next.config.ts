import { defineCloudflareConfig } from "@opennextjs/cloudflare";

// Default Cloudflare config. Uncomment/extend if you later need R2-backed
// ISR caching or other overrides — see https://opennext.js.org/cloudflare/caching
export default defineCloudflareConfig();
